import React, { FC } from "react";
import { FormWrapper, FormContainer } from "../components";

const Login: FC = () => {
   return (
      <FormWrapper>
         <FormContainer/>
      </FormWrapper>
   )
}

export default Login;